# Leduc Hold'em CFR+ Solver Dependencies
# Install with: pip install -r requirements.txt

# Build & bindings
pybind11>=2.10.0       # C++/Python bindings
setuptools>=42.0.0     # C++ extension build

# Visualization
matplotlib>=3.5.0      # Convergence plots
