# TiltStack DeepCFR Dependencies
# Install with: pip install -r requirements.txt
# Note: PyTorch must be installed separately with appropriate CUDA support
# See environment.yml for conda-based setup (recommended)

# Core dependencies
numpy                    # Array operations, linear algebra
matplotlib              # Visualization (convergence plots, diagnostics)
pybind11               # C++/Python bindings (hand_indexer, deepcfr)
setuptools>=42.0.0     # C++ extension build support

# GPU & ML (required for training)
torch                  # PyTorch (install separately for CUDA support)
faiss-gpu             # GPU-accelerated K-means (clustering pipeline)
open_spiel            # Game simulation library (evaluation only)
